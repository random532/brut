- A GUI (X11, gtk3) for:
- create, delete, edit disks/slices/partitions
- create file systems
- write .img files to disk.

To start it, run "xdisk".
If it complains about a missing machine-id, run "dbus-uuidgen --ensure".
